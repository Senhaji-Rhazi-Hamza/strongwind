apiVersion: 1
providers:
  - name: gpu-observability-provider
    orgId: 1
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
